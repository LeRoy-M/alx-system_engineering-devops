## 0x12. Web stack debugging #2

**0. Run software as another user** `[0-iamsomeoneelse]` >> Bash script that accepts one argument and runs the `whoami` command under the user passed as an argument.

**1. Run Nginx as Nginx** `[1-run_nginx_as_nginx]` >> Bash script that configures the container to have `nginx` run as `nginx` user and `nginx` listens on all active IPs on port `8080`.

**2. 7 lines or less** `[100-fix_in_7_lines_or_less]` >> Bash script that performs same operation as task #1, but in 7 or less lines.
